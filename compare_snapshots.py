"""
Compare Snapshots Entry Point

This script serves as the entry point for the Compare Snapshots module.
It imports and runs the main loop from the compare_snapshots package.
"""

from compare_snapshots import run_loop

if __name__ == "__main__":
    # Run the main loop
    run_loop()
